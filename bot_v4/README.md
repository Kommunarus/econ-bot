# 🤖 Бот с продвинутыми возможностями (версия 4)

Добро пожаловать в `bot_v4` — улучшенную версию нашего чат-бота с поддержкой кеширования, метрик и модульной архитектурой!

## 🧠 Основные особенности

- **Агент на базе Langchain**: Интеллектуальная обработка запросов с использованием LLM.
- **Кеширование**: Повышение скорости ответов и снижение нагрузки на API.
- **Метрики**: Сбор статистики по использованию бота.
- **Модульность**: Четкое разделение логики на независимые компоненты.
- **Логирование**: Отслеживание всех действий для отладки и анализа.

## 📁 Структура проекта
```text
bot_v4/
├── cache/                # Кешированные данные (LLM и ретривер)
├── knowledge_base/       # База знаний бота (текстовые файлы)
├── logs/                 # Логи и метрики
├── agent.py              # Логика агента
├── caching.py            # Модуль кеширования
├── main.py               # Точка входа
├── metrics.py            # Сбор метрик
├── tools.py              # Инструменты для агента
├── .env.example          # Пример переменных окружения
└── requirements.txt      # Зависимости
```


## 🚀 Быстрый старт

1. **Установите зависимости**:
   ```bash
   pip install -r requirements.txt
   ```

2. **Настройте окружение**:
   Скопируйте `.env.example` в `.env` и заполните необходимые переменные.

3. **Запустите бота**:
   ```bash
   python main.py
   ```

## 🛠 Компоненты

### `main.py`
Точка входа в приложение. Запускает агента и обрабатывает входящие сообщения.

### `agent.py`
Содержит логику агента, который использует LLM и инструменты для генерации ответов.

### `tools.py`
Набор инструментов, доступных агенту:
- Поиск информации о компании и продуктах
- Вычисления (умножение цен и количества товаров)

### `caching.py`
Реализует кеширование для LLM и ретривера, что значительно ускоряет обработку повторяющихся запросов.

### `metrics.py`
Собирает и сохраняет метрики использования бота (например, количество обработанных сообщений).

## 📚 База знаний

Файлы в `knowledge_base/` содержат информацию, которую бот использует для ответов:
- `company_info.txt`: Описание компании
- `products.txt`: Информация о товарах

## 📈 Метрики и логи

- `logs/chat_session.log`: Подробный лог всех сообщений.
- `logs/metrics.json`: Статистика использования бота.

## 🧪 Кеширование

Кеши хранятся в папке `cache/`:
- `cache_llm.json`: Кешированные ответы LLM.
- `cache_retriever.pkl`: Кешированные результаты ретривера.